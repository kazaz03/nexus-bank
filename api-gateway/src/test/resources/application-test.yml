spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: false

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false

jwt:
  secret: nexusBankSecretKey2025VeryLongAndSecureForHMACSHA256Algorithm
